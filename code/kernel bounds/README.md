# Kernel-bound plots

This folder contains the self-contained MATLAB code for the true-kernel, pointwise-bound, and support-uniform-bound comparison plots.

Run from this folder:

```matlab
out = run_bounds_true_kernel_repro();
```

The script returns and plots only the three paper-relevant quantities: true kernel magnitude, pointwise bound, and support-uniform bound.
